# iPinYou DuckDB 파이프라인 환경 변수 샘플
#
# 아래 값은 .env에 복사해 사용하세요. 각 항목은 기본값과 함께 설명되어 있습니다.

# 1. 필수 경로 설정
# 원본 iPinYou 데이터셋 루트 (training*/testing* 디렉터리가 있는 위치)
IPINYOU_DATASET_ROOT=/mnt/workspace/ipinyou_dataset/datasets/lastsummer/ipinyou/versions/1/ipinyou.contest.dataset/

# 파이프라인 결과 출력 디렉터리 (train.tsv/test.tsv, decompressed 캐시 등 저장)
IPINYOU_OUTPUT_DIR=/home/devcomfort/make-ipinyou-data/all_duckdb

# 스키마 파일 경로 (기본값: schema.txt)
#IPINYOU_SCHEMA_FILE=schema.txt

# 2. DuckDB 설정
# 메모리 상한 (예: 8GB, 2GB 등) – 미설정 시 DuckDB 기본값 사용
# IPINYOU_DUCKDB_MEMORY_LIMIT=8GB

# DuckDB 파일 경로 (미설정 시 in-memory)
#IPINYOU_DUCKDB_FILE=/home/user/ipinyou.duckdb

# DuckDB 스레드 수 (기본: DuckDB 자동 설정)
#IPINYOU_DUCKDB_THREADS=8

# 3. 압축 해제/캐시 설정
# bz2 해제 파일을 저장할 디렉터리 (기본: {OUTPUT_DIR}/decompressed)
#IPINYOU_DECOMPRESSED_DIR=/home/devcomfort/make-ipinyou-data/all_duckdb/decompressed

# 해제 시 사용할 워커 수 (기본: CPU-1)
#IPINYOU_DECOMPRESS_WORKERS=8

# 이미 해제된 파일을 다시 해제할지 여부 (1=true, 0=false; 기본 false)
#IPINYOU_FORCE_DECOMPRESS=0

# 4. 출력/로깅 옵션
# 학습 TSV 출력 경로 (기본: {OUTPUT_DIR}/train.tsv)
#IPINYOU_TRAIN_OUTPUT=/home/devcomfort/make-ipinyou-data/all_duckdb/train.tsv

# 테스트 TSV 출력 경로 (기본: {OUTPUT_DIR}/test.tsv)
#IPINYOU_TEST_OUTPUT=/home/devcomfort/make-ipinyou-data/all_duckdb/test.tsv

# 광고주 컬럼명 (기본: advertiser)
#IPINYOU_ADVERTISER_COLUMN=advertiser

# 광고주별 TSV도 생성할지 여부 (1=true, 0=false; 기본 false)
#IPINYOU_SPLIT_ADVERTISERS=0

# 진행률 로그 출력 여부 (1=true, 0=false; 기본 false)
IPINYOU_PROGRESS=1

# 상세 로그 출력 여부 (1=true, 0=false; 기본 false)
IPINYOU_VERBOSE=1